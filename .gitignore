# secrets — .env holds the actual API key, .env.example is the committed template
.env
.env.local

# binaries
/ledger
/bin/
cmd/ledger/ledger

# real SMS dumps must never be committed
testdata/*.xml
*.dump
*.dump.xml

# editors
.idea/
.vscode/
*.swp

# OS
.DS_Store
